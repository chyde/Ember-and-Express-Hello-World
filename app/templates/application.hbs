<h1>PIZZA</h1>
  {{#if session.isAuthenticated}}
    <button {{action 'invalidateSession'}}>Logout</button>
  {{else}}
    {{#link-to 'login'}}Login{{/link-to}}
  {{/if}}

{{#link-to "pizza" }}List of Pizzas{{/link-to}}

{{outlet}}